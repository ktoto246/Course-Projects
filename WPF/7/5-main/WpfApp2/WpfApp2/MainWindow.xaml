<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учет IT-Активов" Height="650" Width="1000" WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Border Grid.Column="0" Background="#1E1E2D">
            <StackPanel Margin="0,20,0,0">


                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#A2A3B7"/>
                        <Setter Property="Height" Value="45"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Padding" Value="20,0,0,0"/>
                        <Setter Property="FontSize" Value="15"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1b1b28"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>


                <TextBlock Text="МЕНЮ АКТИВОВ" Foreground="#92929F" FontSize="14" FontWeight="Bold" Margin="20,10,20,20"/>

                <Button Content="Отделы" Click="NavОтделы_Click"/>
                <Button Content="Рабочие места" Click="NavРабочиеМеста_Click"/>
                <Button Content="Оборудование" Click="NavОборудование_Click"/>
                <Button Content="Программное обеспечение" Click="NavПО_Click"/>
                <Button Content="Установки ПО" Click="NavУстановки_Click"/>
                <Button Content="Статистика" Click="NavСтатистика_Click"/>
            </StackPanel>
        </Border>


        <Border Grid.Column="1" Background="White" Margin="15" CornerRadius="10">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Margin="10"/>
        </Border>
    </Grid>
</Window>