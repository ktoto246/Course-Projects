<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учет нефтебазы" Height="600" Width="1000" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1E1E1E">
            <StackPanel Margin="10, 20, 10, 10">
                <TextBlock Text="МЕНЮ" Foreground="White" FontSize="18" FontWeight="Bold" Margin="15,0,0,25" HorizontalAlignment="Left"/>

                <Button Name="BtnProducts" Content="Нефтепродукты" Style="{StaticResource SidebarButtonStyle}" Click="BtnProducts_Click"/>
                <Button Name="BtnClients" Content="Клиенты" Style="{StaticResource SidebarButtonStyle}" Click="BtnClients_Click"/>
                <Button Name="BtnIncomes" Content="Приход (Поступления)" Style="{StaticResource SidebarButtonStyle}" Click="BtnIncomes_Click"/>
                <Button Name="BtnSales" Content="Расход (Реализация)" Style="{StaticResource SidebarButtonStyle}" Click="BtnSales_Click"/>

                <Border Height="1" Background="#333337" Margin="10,15,10,15"/>

                <Button Name="BtnCharts" Content="Графики и Статистика" Style="{StaticResource SidebarButtonStyle}" Click="BtnCharts_Click"/>
            </StackPanel>
        </Border>

        <Frame Grid.Column="1" Name="MainFrame" NavigationUIVisibility="Hidden" Background="#F5F5F5"/>
    </Grid>
</Window>