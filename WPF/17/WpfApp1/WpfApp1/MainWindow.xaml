<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1200">
    <Window.Resources>
        <Style TargetType="Button" x:Key="NavButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E1E8ED"/>
                    <Setter Property="Foreground" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock" x:Key="HeaderTextStyle">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="20,0"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#005A9E">
            <TextBlock Text="Прихоперское ПО филиала ПАО «Россети Волга» - «Саратовские РС»" Style="{StaticResource HeaderTextStyle}"/>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
                <StackPanel Margin="0,20,0,0">
                    <Button Content="📊 Сводка (Обзор)" ToolTip="Общая статистика по системе" Style="{StaticResource NavButtonStyle}" Click="BtnDashboard_Click"/>
                    <Button Content="⚡ Инциденты" ToolTip="Управление таблицей incidents" Style="{StaticResource NavButtonStyle}" Click="BtnIncidents_Click"/>
                    <Button Content="👷 Бригады" ToolTip="Управление таблицей brigades" Style="{StaticResource NavButtonStyle}" Click="BtnBrigades_Click"/>
                    <Button Content="📝 Журнал выездов" ToolTip="Управление таблицей work_logs" Style="{StaticResource NavButtonStyle}" Click="BtnWorkLogs_Click"/>
                    <Separator Margin="10,15" Background="#E0E0E0"/>
                    <Button Content="🗺️ Районы (РЭС)" ToolTip="Управление таблицей districts" Style="{StaticResource NavButtonStyle}" Click="BtnDistricts_Click"/>
                    <Button Content="🔌 Типы оборудования" ToolTip="Управление таблицей equipment_types" Style="{StaticResource NavButtonStyle}" Click="BtnEquipment_Click"/>
                    <Separator Margin="10,15" Background="#E0E0E0"/>
                    <Button Content="📈 Отчеты" ToolTip="Графики и аналитика" Style="{StaticResource NavButtonStyle}" Click="BtnReports_Click"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#F4F6F9" Margin="20">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>
    </Grid>
</Window>
