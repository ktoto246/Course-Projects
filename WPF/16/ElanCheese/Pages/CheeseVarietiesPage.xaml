<Page x:Class="ElanCheeseApp.Pages.CheeseVarietiesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="680" d:DesignWidth="1000"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display"
      Title="Сорта сыра"
      Background="{StaticResource BackgroundColor}">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Сорта сыра" Style="{StaticResource HeaderTextStyle}"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="420"/>
            </Grid.ColumnDefinitions>
            
            <!-- DataGrid -->
            <Border Style="{StaticResource CardStyle}">
                <DataGrid x:Name="VarietiesDataGrid" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="OnVarietiesSelectionChanged"
                          Margin="0,0,8,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Созревание (дни)" Binding="{Binding MaturationDays}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            
            <!-- Панель управления -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Управление" Style="{StaticResource HeaderTextStyle}"/>
                    
                    <Label Content="Название:" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="NameTextBox" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,12"/>
                    
                    <Label Content="Созревание (дни):" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="MaturationDaysTextBox" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,12"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="Добавить" Style="{StaticResource ButtonStyle}" Click="OnAddClick"/>
                        <Button Content="Сохранить" Style="{StaticResource ButtonStyle}" Click="OnSaveClick"/>
                        <Button Content="Удалить" Style="{StaticResource DangerButtonStyle}" Click="OnDeleteClick"/>
                        <Button Content="Очистить" Style="{StaticResource ClearButtonStyle}" Click="OnClearClick"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
