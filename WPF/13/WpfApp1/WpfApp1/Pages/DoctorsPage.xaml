<Page x:Class="WpfApp1.Pages.DoctorsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Врачи" Background="White">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Данные врача" Margin="0,0,0,15" Padding="10">
            <StackPanel>
                <UniformGrid Columns="3" Margin="0,0,0,10">
                    <StackPanel Margin="5,0">
                        <TextBlock Text="ФИО врача:"/>
                        <TextBox x:Name="TxtFullName" Padding="3"/>
                    </StackPanel>
                    <StackPanel Margin="5,0">
                        <TextBlock Text="Специальность:"/>
                        <ComboBox x:Name="CmbSpecialization" DisplayMemberPath="Name" SelectedValuePath="Id" Padding="3"/>
                    </StackPanel>
                    <StackPanel Margin="5,0">
                        <TextBlock Text="Кабинет:"/>
                        <TextBox x:Name="TxtCabinet" Padding="3"/>
                    </StackPanel>
                </UniformGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Сохранить" Click="SaveBtn_Click" Width="100" Margin="5" Background="#28A745" Foreground="White" FontWeight="Bold"/>
                    <Button Content="Новый" Click="ClearBtn_Click" Width="100" Margin="5" Background="#6C757D" Foreground="White"/>
                    <Button Content="Удалить" Click="DeleteBtn_Click" Width="100" Margin="5" Background="#DC3545" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Поиск (ФИО):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="SearchTextBox" Width="250" Padding="3" TextChanged="SearchTextBox_TextChanged" Margin="0,0,20,0"/>

            <TextBlock Text="Специальность:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="FilterSpecCombo" DisplayMemberPath="Name" Width="150" Padding="3" SelectionChanged="FilterSpecCombo_SelectionChanged"/>
        </StackPanel>

        <DataGrid x:Name="DoctorsGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" 
                  SelectionChanged="DoctorsGrid_SelectionChanged" RowHeight="30">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Специальность" Binding="{Binding Specialization.Name}" Width="*"/>
                <DataGridTextColumn Header="Кабинет" Binding="{Binding CabinetNumber}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>