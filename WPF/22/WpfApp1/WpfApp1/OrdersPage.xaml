<Page x:Class="WpfApp1.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrdersPage">
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,0">
            <TextBlock Text="ПОИСК ПО ЗАКАЗАМ:" Foreground="DarkGray" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Name="TxtSearch" Width="300" Height="30" Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="5" TextChanged="TxtSearch_TextChanged"/>
        </StackPanel>

        <DataGrid Name="OrdersGrid" Grid.Row="1" 
                  AutoGenerateColumns="False" CanUserAddRows="False" 
                  Background="#1E1E1E" RowBackground="#252526" Foreground="White" 
                  BorderBrush="#3E3E42" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#3E3E42"
                  Margin="10" IsReadOnly="True" SelectionMode="Single"
                  SelectionChanged="OrdersGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№ Заказа" Binding="{Binding OrderId}" Width="80"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding OrderDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client.ContactName}" Width="*"/>
                <DataGridTextColumn Header="Ответственный" Binding="{Binding Employee.FullName}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Background="#252526" Margin="10,0">
            <TextBlock Text="ДЕТАЛИ ВЫБРАННОГО ЗАКАЗА" Foreground="DarkGray" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                <Button Name="BtnAddDetail" Content="+ Добавить услугу" Width="130" Height="30" Background="#2E7D32" Foreground="White" BorderThickness="0" Margin="0,0,5,0" Click="BtnAddDetail_Click"/>
                <Button Name="BtnDeleteDetail" Content="- Удалить услугу" Width="130" Height="30" Background="#C62828" Foreground="White" BorderThickness="0" Click="BtnDeleteDetail_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid Name="DetailsGrid" Grid.Row="3" 
                  AutoGenerateColumns="False" CanUserAddRows="False" 
                  Background="#1E1E1E" RowBackground="#252526" Foreground="White" 
                  BorderBrush="#3E3E42" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#3E3E42"
                  Margin="10" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Услуга" Binding="{Binding Service.ServiceName}" Width="*"/>
                <DataGridTextColumn Header="Цена за ед." Binding="{Binding Service.BasePrice}" Width="120"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>