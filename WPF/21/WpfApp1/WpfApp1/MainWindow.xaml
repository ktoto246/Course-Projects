<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Балашовский комбинат - Лабораторный контроль" Height="650" Width="1100"
        Background="#121212" WindowStartupLocation="CenterScreen" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button" x:Key="MenuButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#B0B0B0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Margin="10,5">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#F5A623"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#181818" BorderBrush="#2A2A2A" BorderThickness="0,0,1,0">
            <StackPanel>
                <TextBlock Text="АГРО КОНТРОЛЬ" Foreground="#F5A623" FontSize="22" FontWeight="Black" Margin="25,35,20,40"/>

                <Button Content="Главная (Графики)" Style="{StaticResource MenuButton}" Click="NavDashboard_Click" />
                <Button Content="Поставщики" Style="{StaticResource MenuButton}" Click="NavSuppliers_Click" />
                <Button Content="Лаборанты" Style="{StaticResource MenuButton}" Click="NavStaff_Click" />
                <Button Content="Показатели качества" Style="{StaticResource MenuButton}" Click="NavIndicators_Click" />
                <Button Content="Приемка зерна" Style="{StaticResource MenuButton}" Click="NavBatches_Click" />
                <Button Content="Журнал анализов" Style="{StaticResource MenuButton}" Click="NavAnalysis_Click" />
            </StackPanel>
        </Border>

        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden" Margin="30"/>
    </Grid>
</Window>